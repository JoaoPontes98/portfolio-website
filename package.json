{
  "name": "portfolio-website",
  "version": "1.0.0",
  "description": "Professional portfolio website for fullstack engineer",
  "main": "index.js",
  "scripts": {
    "start": "react-scripts start",
    "build": "echo 'Node.js version:' && node --version && echo 'npm version:' && npm --version && echo 'Starting build...' && react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "styled-components": "^6.1.1",
    "framer-motion": "^10.16.4",
    "react-icons": "^4.12.0",
    "aws-sdk": "^2.1490.0"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@types/styled-components": "^5.1.26"
  },
  "engines": {
    "node": "20.x"
  }
}
